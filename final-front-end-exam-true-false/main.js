// Question4 : True Or False
// 1- JavaScript is synchronous, blocking, single-threaded language.
true;

// 2- With interpolation, Angular Converts the expression results to strings.

// 3- Javascript provides a parameterless constructor for each class.
false;

// 4- A method inside an abstract class must be declared abstract
true;

// 5- Two formal parameters for the same method may use the same name in Javascript
false;

// 6- A class may extend only one other class and implement only one interface
false;

// 7- If class A extends class B, class A is a subclass of B and B is a superclass of A.
true;

// 8- Encapsulation is the concept of object-oriented programming that “shows” only essential attributes and “hides” unnecessary information.
false; // ==> unnecessary menas abs

// 9- Elements that have higher z-index values are displayed in front of elements with lower z-index values.
true;

// 10- Enums or enumerations are a TypeScipt data type that allows us to define a set of named constants
true;
